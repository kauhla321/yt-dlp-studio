# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# electron-builder output
/release

# misc
.DS_Store
*.pem
.vscode/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env*.local
.env

# runtime app data (download history, settings, playlist state)
/data/

# build artifact (tsc --noEmit --incremental)
tsconfig.tsbuildinfo

# dev tool binaries (resolved next to the app folder at runtime)
/yt-dlp.exe
/ffmpeg.exe

# agent tooling (not project content)
/.claude/
/.codewhale/

# internal design scratch (kept out of the public repo)
/UI REQUIRMENT/
